{
  "name": "feature-exm",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "dist/main.js",
  "types": "dist/types/main.d.ts",
  "files": [
    "dist"
  ],
  "sideEffects": [
    "**/*.css"
  ],
  "publishConfig": {
    "access":"restricted" 
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "build:showConfig": "tsc --p tsconfig.lib.json --showconfig",
    "lint": "eslint . && tsc -b",
    "preview": "vite preview",
    "test": "jest --runInBand"
  },
  "peerDependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}