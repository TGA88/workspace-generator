import { setupProcess } from '../logic/routeSteps.logic';
import { makeTelemetryEndpoint } from '../../../../shared/utils/endpoint-helpers';

export const __verb____Domain__Endpoint = makeTelemetryEndpoint(setupProcess);
